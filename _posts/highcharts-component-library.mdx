---
title: 'Find the best way to build highcharts components into library'
date: 'January 17, 2022'
description: '1232131313123123'
thumbnailUrl: '/assets/blog/highcharts-component-library/highcharts-component-library.jpeg'
tags: ['functions', 'javascript']
---


123456

# hello

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```