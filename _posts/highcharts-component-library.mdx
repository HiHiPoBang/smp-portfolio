---
title: Find the best way to build highcharts components as library
date: 'January 16, 2022'
description: Find the best way to build highcharts components as library
thumbnailUrl: '/assets/blog/highcharts-component-library/highcharts-component-library.jpeg'
tags: ['functions', 'javascript']
---


123456

# hello

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```

```js
const convertRemToPixel = (rem: string) => R.pipe(
  R.replace(/rem/, ''),
  (val) => Number(val),
  (val) => R.divide(val, 0.0625),
)(rem);
```